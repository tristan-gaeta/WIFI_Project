Use this folder as a starting point. You'll need to configure the
classpath in VS Code before this will compile: From the Command
Palette, select "Java: Configure Classpath", and make sure that
classFiles.jar is included. You'll also need to use the -cp command-
line argument when compiling the project.